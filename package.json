{
  "dependencies": {
    "@types/chess.js": "^0.13.4",
    "chess.js": "^1.0.0-beta.6",
    "chessboardjsx": "^2.4.7",
    "concurrently": "^8.2.0",
    "jointjs": "^3.7.4",
    "package.json": "^2.0.1",
    "react-chessboard": "^3.1.0",
    "serve": "^12.0.0"
  },
  "scripts": {
    "install:client": "cd client && npm ci",
    "build:client": "cd client && npm run build",
    "install:server": "cd server && npm ci",
    "start:server": "cd server && node server.js",
    "build": "npm run install:client && npm run build:client && npm run install:server && npm run start:server"
  }
}
